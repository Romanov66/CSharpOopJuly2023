namespace Test
{
    internal class HashTable
    {
        /*public void Add(List<T> item)
        {
            if (item == null)
            {
                throw new ArgumentNullException("Переданнный аргумен не может быть null", nameof(item));
            }

            lists[Count] = item;

            Count++;
            modCount++;
        }
            if (array is null)
            {
                throw new ArgumentNullException(nameof(array), "Массив не может быть null");
            }

            if (index < 0)
            {
                throw new ArgumentOutOfRangeException(nameof(index), $"Индекс не должен быть меньше нуля. Индекс = {index}");
            }

            if (Count > array.Length)
            {
                throw new ArgumentException("Количество компонентов исходного списка больше, чем конечный индекс массива", nameof(array));
            }

            Array.Copy(lists, 0, array, 0, lists.Length);*/
    }
}
